/**
 * Add types (that are not auto-magically added by Quasar CLI already)
 * for your custom variables here, to avoid TypeScript errors, like dynamic
 * import.meta.env variables or definitions in dotenv files configured ONLY
 * for the /quasar.config file itself.
 *
 * No custom variables are currently declared here; Quasar CLI's own
 * generated .quasar/quasar.d.ts already supplies ImportMetaEnv.
 *
 * @example
 * interface ImportMetaEnv {
 *   readonly MY_VAR: string;
 * }
 */
